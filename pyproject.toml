[project]
name = "snowflake-document-agent"
version = "0.1.0"
description = "Python ELT processes to incrementally load document files into Snowflake Document Agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
